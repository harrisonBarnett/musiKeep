extends layout 

block content 
    h1 _ALBUMS

    each album in albums
        .album-group 
            .album-info
                a(href=(album.url)) _#{album.title}
                a(href=('/artists/' + album.artist._id)) #{album.artist}
                p _#{album.artist_id}